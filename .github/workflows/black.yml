name: Py Lint Lint Lint

on:
  pull_request:
    paths-ignore:
      - 'typescript/**'
  push:
    branches:
      - main
    paths-ignore:
      - 'typescript/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
        with:
          options: "--check --verbose"
          src: "."
          jupyter: true
          # Exact pin: match the Black used locally (`black --version`)
          version: "25.12.0"
