How correct are the arguments of the MCP calls in `primitives_used`: do they match each primitive's input schema in `mcp_servers` (required fields present, right names and types, well formed) and carry the values `input` calls for?