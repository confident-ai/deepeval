---
id: classifiers-clarification
title: ClarificationClassifier
sidebar_label: Clarification
languages: [python]
---

The `ClarificationClassifier` labels a single response as `asked_clarification`, `answered_directly`, or `guessed` based on whether the assistant asked a clarifying question on an ambiguous input, answered because the input was clear enough, or proceeded on an assumption it should not have made. It is meant for agents that take actions such as bookings, purchases, or data changes, where guessing on ambiguous input is costly and the expected label on an underspecified prompt is `asked_clarification`.

## Labels

- `asked_clarification`: the input is ambiguous or underspecified and the response asks a clarifying question before proceeding.
- `answered_directly`: the input is clear enough and the response answers or acts on it without needing clarification.
- `guessed`: the input is ambiguous or underspecified but the response proceeds on an assumption instead of asking.

## Usage

```python
from deepeval.classifiers import ClarificationClassifier
from deepeval.test_case import LLMTestCase
from deepeval import evaluate

clarification = ClarificationClassifier()

test_case = LLMTestCase(
    input="Book me a flight to Portland next week.",
    actual_output="Portland, Oregon or Portland, Maine? And which day next week works best?",
    expected_labels={"clarification": "asked_clarification"},
)

evaluate(test_cases=[test_case], classifiers=[clarification])
```

All built-in classifiers accept the same optional arguments as a [custom `Classifier`](/docs/classifiers-classifier#usage): `model`, `include_reason`, `async_mode`, and `evaluation_template`. You can also override `name` or pass your own `labels`.
